Maven Repository for compiled JD artifacts
==========================================

This repository holds the POMs for JD compiled artifacts. In addition it holds any dependencies which cannot themselves be retrieved from Maven Central.
